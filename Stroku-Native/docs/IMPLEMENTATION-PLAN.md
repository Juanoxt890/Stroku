# Plan de implementación versionado

| Versión | Contenido |
|---|---|
| v7 | Restore cac7bed + marca Stroku890 |
| v8–v11 | Home premium incremental |
| **v12** | Paquete docs A–H + restyle global pantallas existentes + zip |

Principio: un zip `stroku890-vN.zip` por release; `manifest` `build_version=N`.
